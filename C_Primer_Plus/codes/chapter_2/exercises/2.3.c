// 此程序用于将年龄转换成天数，并输出年龄和天数
#include <stdio.h>
int main(void)
{
    int age, days;
    age = 18;
    days = 18 * 365;
    printf("My age is %d years, which equals to %d days", age, days);
    printf("fuck");
    return 0;
}