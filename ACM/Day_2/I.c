#include <stdio.h>

int dp[1005];
int value[1005];

int main(void)
{
    int n, m;
    
    while (scanf("%d",&n) == 1) {
        for (int i = 0; i < n; i++)
            scanf("%d", &value[i]);
        scanf("%d", &m);

        
    }
}